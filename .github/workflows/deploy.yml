name: Build and Deploy SIG App

on:
  push:
    branches:
      - main   # Déclenche quand tu push sur main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Récupérer ton code
      - name: Checkout code
        uses: actions/checkout@v3

      # 2. Construire l'image Docker
      - name: Build Docker image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/sigapp:latest .

      # 3. Se connecter à Docker Hub
      - name: Login to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      # 4. Pousser l'image sur Docker Hub
      - name: Push Docker image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/sigapp:latest

      # 5. Déployer sur la VM Azure via SSH
      - name: Deploy on Azure VM
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.AZURE_VM_IP }}
          username: ${{ secrets.AZURE_VM_USER }}
          key: ${{ secrets.AZURE_VM_SSH_KEY }}
          script: |
            docker pull ${{ secrets.DOCKER_USERNAME }}/sigapp:latest
            docker stop sigapp || true
            docker rm sigapp || true
            docker run -d -p 80:80 --name sigapp ${{ secrets.DOCKER_USERNAME }}/sigapp:latest
